# Database settings.
database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./target/db

# Logging settings.
logging:
  level: INFO
  loggers:
    io.dunham.raku: DEBUG
    org.hibernate.SQL:
      level: DEBUG
