# File uplaod
filesDir: ./data/files

# Database location
databaseLocation: ./data/db

# Logging settings.
logging:
  level: INFO
  loggers:
    io.dunham.raku: DEBUG
    org.hibernate.SQL:
      level: DEBUG
    io.dropwizard.jersey.jackson: DEBUG
